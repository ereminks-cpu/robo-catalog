# ROBO Catalog v9.3 — Decimal Comma Fix

Исправлено:
- в карточке робота теперь можно вводить десятичные значения через запятую:
  - 1,5
  - 10,5
  - 0,75
- браузер больше не показывает ошибку `Введите допустимое значение`.
- числовые поля карточки переведены с `type="number"` на `type="text" inputmode="decimal"`.

На GitHub заменить:
- index.html
- style.css
- README.md

SQL повторять не нужно.
После публикации: Cmd + Shift + R.
