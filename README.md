# ROBO Catalog v9.17 — Robot Delete Fix

Исправлено:
- удаленный робот больше не возвращается через секунду;
- причина была в том, что `closeCard()` при закрытии карточки автоматически сохранял форму и заново создавал удаленного робота через `upsert`;
- теперь при удалении карточка закрывается без автосохранения;
- добавлена контрольная проверка удаления из Supabase.

На GitHub заменить:
- index.html
- style.css
- README.md

SQL повторять не нужно.
